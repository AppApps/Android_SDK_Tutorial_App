<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.V14.Theme.AppCompat">
        <item name="android:typeface">monospace</item>
        <item name="android:actionBarItemBackground">@color/primary_dark_material_dark</item>
        <item name="android:actionModeBackground">@color/primary_dark_material_dark</item>
    </style>

</resources>
